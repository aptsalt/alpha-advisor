# ── core (local demo: mock & ollama, CLI + tests) ────────────────────────────
langgraph>=1.0
langgraph-checkpoint>=2.0
numpy>=1.26
networkx>=3.2
httpx>=0.27
python-dotenv>=1.0

# ── service (API / deploy) ───────────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30

# ── observability (optional; ALPHA_TRACING=1) ────────────────────────────────
opentelemetry-sdk>=1.27
opentelemetry-api>=1.27
# opentelemetry-exporter-otlp-proto-http   # when shipping to a collector / Azure Monitor

# ── production backends (optional; activated by env, fall back if absent) ─────
# langgraph-checkpoint-postgres            # ALPHA_CHECKPOINT_DB=postgresql://…
# neo4j                                    # ALPHA_GRAPH_DB=bolt://…
# azure-search-documents / chromadb        # managed vector store swap
